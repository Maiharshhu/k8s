apiVersion: v1
kind: ReplicationController
metadata:
  name: myjavaapp-rc  
spec:
  replicas: 3
  selector:
    app: myjavaapp
  template:
    metadata:
      labels: 
        app: myjavaapp
        version: 2.6.1        
    spec:
      containers:
        - name: mycontainer
          image: maha4iac/mahalogin:1
          ports:
            - containerPort: 8080
