apiVersion: v1
kind: Service
metadata:
  name: harsh-svc
  labels:
    app: myjavaapp
spec:
  type: NodePort
  ports:
  - port: 8080
    NodePort: 30001
    Protocol: TCP
  selector:
    app: myjavaapp
   
  