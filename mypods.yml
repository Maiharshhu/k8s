apiVersion: v1
kind: Pod
metadata:
  name: mynginxpod
  labels:
    name: mynginxpod
spec:
  containers:
  - name: mynginxpod
    image: nginx
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 30001
        targetport: 80
